--
-- Copyright 2009 Glencoe Software, Inc. All rights reserved.
-- Use is subject to license terms supplied in LICENSE.txt
--

-- This file was generated by dsl/resources/ome/dsl/enums_update.vm

#foreach($enum in $types)
#foreach($prop in $enum.properties)
#if($prop.class.name == "ome.dsl.EntryField")
#set($table = ${enum.typeToColumn($enum.id)} )
insert into ${table} (id,permissions,owner_id,group_id,creation_id,value)
    select ome_nextval('seq_${table}'),-35,0,0,0,'${prop.name}' from ${table} where not exists(
        select 1 from ${table} where value = '${prop.name}') limit 1;

#end
#end
#end
