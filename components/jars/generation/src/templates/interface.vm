#set ($file = "interface.vm")

#if( $package )
package ${package}.interfaces;
#end

/** autogenerated by Velocity template $file */

public interface I${class.LocalName}
#if ($class.SuperClass)
#set ($super = $class.SuperClass)
   extends I${super.LocalName}  // $super.URI
#else
   extends org.ome.ILSObject ## TODO but not SemanticType
#end
{

	public final static String URI="${class.URI}";
	public String getURI();

#foreach( $p in $class.Predicates )
 	//-----------------------------------
	// $p
	#set ( $t = $p.Type )
	#set ( $n = $p.LocalName )
	public $t get${util.uc(${n})}();
	public boolean set${util.uc(${n})}($t value);
#end
}
