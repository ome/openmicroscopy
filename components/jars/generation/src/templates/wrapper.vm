#set ($file = "wrapper.vm") 
## FIXME DEPRECATED
#if( $package )
  package ${package}.model;
#end
  import org.ome.model.LSObject;
  import java.util.Iterator;
  import java.util.ArrayList;
  import java.util.List;

/** autogenerated by Velocity template $file */

public class ${class.LocalName}Wrapper
{
/* FIXME Do we need this? This or Factory.
	public static ${class.LocalName} wrap(LSObject lsobj){
		return new ${class.LocalName}(lsobj);
	}
*/
	public static List wrap(List lsObjects){
		List results = new ArrayList();
		for (Iterator iter = lsObjects.iterator();iter.hasNext();){
			results.add(new ${class.LocalName}( (LSObject)iter.next() ));
		}
		return results;
	}

}
