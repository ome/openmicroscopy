#set ($file = "schema.vm") 
<?xml version="1.0"?>
<!-- file generated by $file -->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
        targetNamespace="http:/www.openmicroscopy.org/2005/schema/dataTransfer"
        xmlns="http:/www.openmicroscopy.org/2005/schema/dataTransfer"
        elementFormDefault="qualified">
 <xs:element name="$class.LocalName">
  <xs:complexType>
   <xs:complexContent>
#if ($class.SuperClass)
  #set ($base = $class.SuperClass.LocalName )
#else   
  #set ($base = "BaseRDFObject")
#end   
    <xs:extension base="$base">
     <xs:sequence>
#foreach( $p in $class.Predicates )
	#set ( $t = $p.XsdType )
	#set ( $n = $p.LocalName )

#if ($p.Min)
    #set ($min = $p.Min )
#else   
    #set ( $min = "0" )
#end   

#if ($p.Max)
    #set ($max = $p.Max )
#else   
    #set ( $max = "unbounded" )
#end   
      <xs:element name="$n" type="$t" minOccurs="$min" maxOccurs="$max"/>
#end 
      <xs:element name="parameters">
      <xs:complexType>
       <xs:sequence>
        <xs:element name="immutable" type="xs:boolean"/>
       </xs:sequence>
      </xs:complexType>
     </xs:element>
    </xs:sequence>
   </xs:extension>
  </xs:complexContent>
 </xs:complexType>
 </xs:element>
</xs:schema>   