#set ($file = "factory.vm")

#if( $package )
package ${package}.model;
#end

/** autogenerated by Velocity template $file */

public class Factory
{
	public static LSObject make (String str){
		LSID lsid = null;
		try {
			lsid = new LSID(str);
		} catch (Exception e){
			return null;
		}

		LSObject lsobj = null;
#foreach( $c in $classes )
#set ( $t = $c.Type )
#set ( $n = $c.LocalName )
    	  if (str.startsWith("$c.URI")) {
    	     lsobj = new $n(lsid);
	      } else
#end
          {
       		 lsobj = new OMEObject(lsid);
	      }

	   return lsobj;

	}

}
