#set ($file = "vocab.vm")

#if( $package )
package ${package}.model;
#end

/** autogenerated by Velocity template $file */

public class Vocabulary
{

#foreach( $p in $predicates )
#set ( $n = $p.LocalName )
	public final static String $n = "$p.URI";
	public final static Integer ${n}_MIN = new Integer($p.Min);
#if ($p.Max)
	public final static Integer ${n}_MAX = new Integer($p.Max);
#else
	public final static Integer ${n}_MAX = null;
#end
#end

	public static Integer getMin(String pred){
		if (null==pred){
			return null;
		}
		
#foreach( $p in $predicates )
#set ( $n = $p.LocalName )
		if (${n}.equals(pred)){
			return ${n}_MIN;
		} else
#end
		{
			return null;
		}
	}

	//TODO these could be in maps to save lookup time.
	public static Integer getMax(String pred){
		if (null==pred){
			return null;
		}
		
#foreach( $p in $predicates )
#set ( $n = $p.LocalName )
		if (${n}.equals(pred)){
			return ${n}_MAX;
		} else
#end
		{
			return null;
		}
	}

}
