#set ($file = "vocab.vm")

#if( $package )
package ${package};
#end

/** autogenerated by Velocity template $file */

public interface Vocabulary
{

#foreach( $p in $predicates )
	public final static String $p.LocalName = "$p.URI";

#end
}
