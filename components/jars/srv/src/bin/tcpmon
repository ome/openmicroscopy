#!/bin/bash

CP=$1
CP=~/lib/axis
if [ -z "$CP" ]; then
	CP=../web/WEB-INF
fi

CL=$CP/classes
LI=$CP/lib
CP=$CP:$CL
for c in $LI/*.jar; do
	CP=$CP:$c
done
shift

/bin/sh -c "java -cp $CP org.apache.axis.utils.tcpmon $*" &

exit 0
