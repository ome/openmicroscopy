*** Settings ***

Resource     ../../config.txt
Resource     ../../../resources/insight/login.txt
Suite Setup  Log In As                              user=${USERNAME}  pass=${PASSWORD}  host=${OMERO HOST}  port=${OMERO PORT}
