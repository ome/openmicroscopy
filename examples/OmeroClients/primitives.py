#!/usr/bin/env python
# -*- coding: utf-8 -*-
from omero.rtypes import rstring, rbool, rlong, rint

string_value = rstring("value")
boolean_value = rbool(True)
long_value = rlong(1)
int_value = rint(1)
