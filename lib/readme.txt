These are the required dependencies for the omero system.

/repository contains the dependencies defined in the various
pom.xml files. It represents a snapshot of a maven2 repository.

/tools contains other libraries needed at various times of the 
build process, but which aren't necessary needed from maven.


